<Page
    x:Class="Nokia_Chat.ConversationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nokia_Chat"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.TopAppBar>
        <CommandBar>
            <AppBarButton Label="刷新" Click="RefreshButton_Click" Icon="Sync"/>
        </CommandBar>
    </Page.TopAppBar>

    <Grid>
        <ListView x:Name="ConversationsListView" IsItemClickEnabled="True" ItemClick="ConversationsListView_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="12,8">
                        <TextBlock Text="{Binding TitleText}" Style="{ThemeResource SubheaderTextBlockStyle}"/>
                        <TextBlock Text="{Binding PreviewText}" Opacity="0.7" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
